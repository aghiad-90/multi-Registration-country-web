import * as Colors from './colors.ts';
// import {getTheme} from '../../handlers.ts';

// export const EG_Theme = getTheme(Colors);

export const EG_Theme = {
    // ...DefaultTheme,
    colors: {
      // ...DefaultTheme.colors,
      primary: Colors.PRIMARY,
      secondary: Colors.SECONDARY,
    },
  };

export default EG_Theme;