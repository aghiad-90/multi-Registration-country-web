import * as Colors from './colors.ts';
// import {getTheme} from '../../handlers.ts';

// export const IN_Theme = getTheme(Colors);

export const IN_Theme = {
    // ...DefaultTheme,
    colors: {
      // ...DefaultTheme.colors,
      primary: Colors.PRIMARY,
      secondary: Colors.SECONDARY,
    },
  };

export default IN_Theme;