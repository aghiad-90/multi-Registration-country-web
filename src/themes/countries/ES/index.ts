// import {getTheme} from '../../handlers.ts';
import * as Colors from './colors.ts';

// export const ES_Theme = getTheme(Colors);

export const ES_Theme = {
    // ...DefaultTheme,
    colors: {
      // ...DefaultTheme.colors,
      primary: Colors.PRIMARY,
      secondary: Colors.SECONDARY,
    },
  };


export default ES_Theme;